test("Ensure when start/stop is pressed, the timer is reset to the max time", () => {
	// Temporary test while the codebase is setup
	expect(1).toBe(1);
})

test("Ensure when start/stop is pressed, text switches to the opposite", () => {
	// Temporary test while the codebase is setup
	expect(1).toBe(1);
})